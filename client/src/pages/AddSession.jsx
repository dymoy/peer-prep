/**
 * @file AddSession.jsx
 * React page that serves the SessionForm Component for the User to create a Session
 */

import SessionForm from '../components/SessionForm';

const AddSession = () => {
    return (
        <SessionForm />
    )
};

export default AddSession;
